[project]
name = "delivery-service"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "packages/shared",
    "src/api-gateway",
    "src/mq-consumer",
]
