[project]
name = "delivery-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.18.5",
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.139.0",
    "psycopg2>=2.9.12",
    "pytest>=9.1.1",
    "sqlalchemy>=2.0.51",
]

[tool.pyright]
extraPaths = ["src/delivery_service"]

[tool.ruff]
src = [".", "src/delivery_service"]
