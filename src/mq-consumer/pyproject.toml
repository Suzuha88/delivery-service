[project]
name = "mq-consumer"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.139.0",
    "shared",
    "uvicorn>=0.51.0",
]

[tool.uv.sources]
shared = { workspace = true }
